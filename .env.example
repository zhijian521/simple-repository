# ============================================
# 资源仓库 - 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填入实际值

# --------------------------------------------
# GitHub API 配置
# --------------------------------------------
# GitHub 个人访问令牌（需要 repo 权限）
# 获取方式：GitHub Settings -> Developer settings -> Personal access tokens -> Tokens (classic)
NUXT_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub 用户名或组织名称
NUXT_GITHUB_OWNER=your_github_username_here

# GitHub 仓库名称
NUXT_GITHUB_REPO=your_repository_name_here

# GitHub 分支名称（可选，默认为 main）
NUXT_GITHUB_BRANCH=main

# --------------------------------------------
# 身份验证配置
# --------------------------------------------
# 登录用户名（用于前端登录）
NUXT_AUTH_USERNAME=admin

# 登录密码
# ⚠️ 生产环境强烈建议使用 bcrypt 哈希而不是明文
# 生成哈希: node -e "console.log(require('bcrypt').hashSync('your-password', 10))"
NUXT_AUTH_PASSWORD=$2b$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# JWT 密钥（用于生成和验证 token）
# ⚠️ 生产环境必须使用强随机密钥
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NUXT_JWT_SECRET=your_jwt_secret_key_here_must_be_very_long_and_secure

# Token 有效期（单位：天，默认为 7 天）
NUXT_TOKEN_EXPIRY_DAYS=7

# --------------------------------------------
# 部署配置（Vercel）
# --------------------------------------------
# 在 Vercel 项目设置中添加这些环境变量
# 确保不要在代码中硬编码这些值！
